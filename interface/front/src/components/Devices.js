const { Link } = require("react-router-dom");

const ListDevices = () => {
    return (
        <div>
            <Link to="/">Home</Link>,
            <h1>Devices Page</h1>
        </div>
    );
};

export default ListDevices;